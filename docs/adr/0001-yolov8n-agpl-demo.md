# ADR-0001: YOLOv8n is acceptable for the demo

YOLOv8n gives a compact, recognizable CV baseline with built-in export paths. The license is AGPL, so this is a demo decision only. For client delivery I would evaluate Apache-2.0 alternatives such as RTMDet and SSD-MobileNet before committing the stack.
