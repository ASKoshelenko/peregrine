# ADR-0003: ARM64-QEMU is not a device farm

QEMU catches packaging, runtime, and gross latency regressions. It does not produce absolute device latency. The site must label it as a trend lane until physical devices or a device farm exist.
